/// obj_bass_shock – Create Event
/// — lifetime in frames (¼ second) —
life    = room_speed * 0.25;
/// — starting opacity —
alpha   = 1;
/// — how far the line reaches (in pixels) —
length  = global.tile_size * 1.5;
// dirX, dirY, origin_x, origin_y will be assigned immediately after you instance_create it
